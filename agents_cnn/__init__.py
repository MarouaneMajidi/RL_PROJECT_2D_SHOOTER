"""
CNN-based Agents Package

This package contains CNN-based implementations of Player and DDA agents
that use raw image inputs instead of manually extracted features.

This is a COMPLETELY SEPARATE implementation from the feature-based agents
in the agents/ directory. The two systems coexist independently.
"""
